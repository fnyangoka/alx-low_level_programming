#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @int: the integer to be computed
 *
 * Return: 0
 */
int _abs(int)
{
	int n;

	if (n >= 0)
		return (n);
	else
		return (-n);
}
